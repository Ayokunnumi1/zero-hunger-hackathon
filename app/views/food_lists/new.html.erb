<div class="container">
  <h1 class="mt-4">New Food List</h1>

  <div class="card">
    <div class="card-body">
      <%= render "form", food_list: @food_list %>
    </div>
  </div>

  <br>

  <%= link_to "Back to food lists", food_lists_path, class: "btn btn-secondary mt-3" %>
</div>
